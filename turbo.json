{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [],
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "env": ["PAYLOAD_PUBLIC_SITE_NAME", "PAYLOAD_PUBLIC_SITE_URL", "PAYLOAD_PUBLIC_PORT", "PAYLOAD_PUBLIC_API_ROUTE", "PAYLOAD_PUBLIC_UPLOAD_ROUTE"]
    },
    "nuxt#build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**"],
      "env": ["NITRO_HOST", "NITRO_PORT"]
    },
    "payloadcms#build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**"],
      "env": ["DATABASE_NAME", "PAYLOAD_SECRET"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "generate": {
      "cache": false
    },
    "payloadcms#generate": {
      "outputs": ["types.d.ts"]
    },
    "nuxt#generate": {
      "dependsOn": ["payload#generate"],
      "outputs": ["graphql/index.js", "graphql/possibleTypes.json"]
    },
    "lint": {},
    "format": {}
  }
}
